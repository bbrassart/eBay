<!DOCTYPE html>
<html>
<head>
  <title>EBay</title>
</head>
<body>
	<div class="row">
		<div class="jumbotron">
			<img src="http://i.imgur.com/zgqxqaT.png">
		</div>
	</div>
	<div class="row">
		<div class="col-md-12">
			<% if current_user %>
				<div class="media current_user--loggedin__info">
                    <div class="media-left">
                    	<a href="<%= user_path(current_user) %>">
                        	<img class="media-object current_user--logo" src="<%=current_user.logo.url(:thumb)%>" alt="User logo">
                        </a>
                    </div>
                    <div class="media-body">
                        <h4 class="media-heading">Hello <%= current_user.name %> </h4>
                        <ul>
                        	<li><%= link_to 'Log out', '/logout'%></li>
							<li><%= link_to 'Edit your profile', "/users/#{current_user.id}/" %></li>
						</ul>
					</div>
				</div>
			<%else%>
				<ul>
					<li>Hello visitor! You can <%= link_to 'sign up here', new_user_path%></li>
					<li><%= link_to 'Log in', '/login'%></li>
				</ul>		
			<%end%>
			<%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
			<%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
			<%= csrf_meta_tags %>

			<% flash.each do |key, value| %>
			<%= content_tag :div, value, class: "flash-info" %>
			<% end %>
			<%= yield %>


		</div>
	</div>
	<footer>
		<%= link_to "List of all the users of eBay", users_path %> 
	</footer>
</body>
</html>
